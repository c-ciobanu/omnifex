> An all-in-one web app for all of my needs

## Local development

- Set your `TMDB_API_ACCESS_TOKEN` environment variable
- Set your `GOOGLE_BOOKS_API_KEY` environment variable
- Run the following commands in your terminal:

```bash
yarn i
yarn dev
```
